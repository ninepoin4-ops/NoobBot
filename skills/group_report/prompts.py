"""LLM Prompt 模板 — 话题提取 / 用户称号 / 金句 / 聊天质量"""
from __future__ import annotations

# ── 话题提取 ──

TOPIC_ANALYSIS_PROMPT = """你是群聊分析专家。分析以下群消息，提取今天最热门的讨论话题。

要求：
1. 话题要具体（不要"日常闲聊"这种泛泛的）
2. 每个话题列出参与讨论的用户ID
3. 简要描述话题内容

消息格式：[HH:MM] [用户ID]: 内容

【消息记录】
{messages}

请以 JSON 格式返回，最多 {max_topics} 个话题：
{{
  "topics": [
    {{"topic": "话题名称", "contributors": ["用户ID1", "用户ID2"], "detail": "话题描述"}}
  ]
}}
只返回 JSON，不要多余文字。"""


# ── 用户称号 ──

USER_TITLE_PROMPT = """你是群聊角色分析师。根据以下用户今日的聊天数据，为每个活跃用户生成一个"称号"、"MBTI类型"和"理由"。

用户数据格式：
- 用户名 (ID): 发言X条, 平均X字, 表情比例X, 夜间发言比例X

要求：
1. 称号要有创意、有趣、贴合用户聊天风格
2. MBTI 根据聊天行为推测
3. 理由简短有趣

【用户数据】
{user_data}

请以 JSON 格式返回：
{{
  "users": [
    {{"name": "用户名", "user_id": "用户ID", "title": "称号", "mbti": "MBTI类型", "reason": "理由"}}
  ]
}}
只返回 JSON，不要多余文字。"""


# ── 金句提取 ──

GOLDEN_QUOTE_PROMPT = """你是群聊金句评审员。从以下消息中，选出当天最精彩、最有趣、最离谱或最经典的发言。

要求：
1. 每条金句必须包含完整内容
2. 说明发送者ID
3. 说明入选理由

消息格式：[HH:MM] [用户ID]: 内容

【消息记录】
{messages}

请以 JSON 格式返回，最多 {max_quotes} 条：
{{
  "quotes": [
    {{"content": "原话内容", "sender": "用户ID", "reason": "入选理由"}}
  ]
}}
只返回 JSON，不要多余文字。"""


# ── 聊天质量锐评 ──

CHAT_QUALITY_PROMPT = """你是一个毒舌又犀利的群聊观察家。分析以下群聊记录，输出一份"聊天质量锐评"。

任务：
1. 将今天的聊天内容划分为 3-5 个抽象维度（如"深夜emo浓度"、"摸鱼技术交流"、"暴躁老哥对线"等）
2. 为每个维度计算占比（总和不超过100%）
3. 给每个维度写一句毒舌点评（幽默、犀利、一针见血）
4. 给一个整体总结

消息格式：[HH:MM] [用户ID]: 内容

【消息记录】
{messages}

请以 JSON 格式返回：
{{
  "title": "今日群聊主题",
  "subtitle": "副标题",
  "dimensions": [
    {{"name": "维度名", "percentage": 比例数字, "comment": "毒舌点评"}}
  ],
  "summary": "一句总结金句"
}}
只返回 JSON，不要多余文字。"""
